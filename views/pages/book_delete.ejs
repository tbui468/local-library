<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body>
    <head>
      <%- include('../partials/header'); %>
    </head>
    <h1><%= title %></h1>
    <h3><%= book.title %></h3>
  
    <% if(locals.bookinstances && bookinstances.length > 0) { %>
      <p>Delete all copies of this book before deleting book</p> 
    <% }else{ %>
      <p>Do you really want to delete this book?</p>
      <form action='' method='POST'>
        <input type='hidden' name='bookid' value='<%= book._id %>'/>
        <button type='submit'>Delete Book</button>
      </form>
    <% } %>

    <h3>Book Copies</h3>
    <ul>
      <% for(let i = 0; i < bookinstances.length; i++) { %>
        <li>
          <p>Status: <%= bookinstances[i].status %></p>
          <p>Imprint: <%= bookinstances[i].imprint %></p>
          <a href="<%= bookinstances[i].url %>">Id: <%= bookinstances[i]._id %></a>
        </li> 
        <br/>
      <% } %>
      <% if(bookinstances.length === 0) { %>
        <p>This book has no copies</p>
      <% } %>
    </ul>
    <footer>
      <%- include('../partials/footer'); %>
    </footer>
  </body>
</html>
